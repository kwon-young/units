:- module(si, [sin/1, cos/1, tan/1, asin/1, acos/1, atan/1, atan2/1]).
:- reexport(si/symbols).

:- use_module(si/constants).
:- use_module(si/prefixes).
:- use_module(si/units).
:- use_module(si/symbols).

sin(si:radian).
cos(si:radian).
tan(si:radian).
asin(si:radian).
acos(si:radian).
atan(si:radian).
atan2(si:radian).
